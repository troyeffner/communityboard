# Heartwood Pointer — `external_evidence_atoms`

Legacy external naming retained for history.

Trunk-first language uses `ether`.
Net-new external expressions should route to:

- `communityboard/ether/evidence-atoms/`

No delete policy is active.
This folder remains as a historical ring/pointer during cutover.

